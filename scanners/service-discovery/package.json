{
  "name": "service-discovery",
  "version": "1.0.0",
  "description": "The plan is to kick off the crawler by scanning the [dns](https://github.com/PHACDataHub/dns) repo, and scan github repo and service endpoints",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "NODE_OPTIONS=--experimental-vm-modules jest --coverage",
    "lint": "eslint src",
    "dbg": "node --experimental-vm-modules --inspect-brk node_modules/.bin/jest --runInBand --no-cache",
    "start": "node --experimental-vm-modules index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "arangojs": "^8.4.0",
    "dotenv": "^16.3.1",
    "fs-extra": "^11.1.1",
    "js-yaml": "^4.1.0",
    "nats": "^2.16.0",
    "octokit": "^3.1.0",
    "simple-git": "^3.19.1"
  },
  "devDependencies": {
    "jest": "^29.6.4"
  }
}
